# -------------------------------------------------------------------
# Terraform
# -------------------------------------------------------------------

# Local Terraform directories (providers, modules cache)
.terraform/

# State files (contain sensitive info)
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.tfstate.lock.info

# Terraform variable files (sensitive data)
*.tfvars
*.tfvars.json

# Terraform override files (local dev only)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files (binary or sensitive outputs)
*.plan
*tfplan*

# Provider lock file (decide: commit or ignore)
# If you want reproducible builds across CI/CD, remove this line
.terraform.lock.hcl

# -------------------------------------------------------------------
# Logs
# -------------------------------------------------------------------
crash.log
crash.*.log

# -------------------------------------------------------------------
# CLI configs (personal AWS creds, not for repo)
.terraformrc
terraform.rc

# -------------------------------------------------------------------
# OS / Editor junk
# -------------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
